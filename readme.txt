- open terminal and type #"docker-compose up" to build the containers .

- now that the containers has been created u can acees the php file via localhost/test.php
    and phpmyadmin via localhost:8001 

